##  Setup & Installation
    pip install -r requirements.txt
    Ensure the `EEGPT` folder is present in the root directory)

### 1. Prepare Data
    Place your dataset folders inside a `raw_data/` folder.

### 2. Run Preprocessing
    python dataset.py

### 3. Training
    python train.py
